{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "bun test src/modules",
    "test:unit": "find src/modules -name '*.spec.ts' -exec bun test {} +",
    "test:integration": "find src/modules -name '*.test.ts' -exec bun test {} +"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "@nubbix/domain": "workspace:*"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.3.0",
    "@nubbix/eslint-config": "workspace:*",
    "@types/bun": "^1.3.5",
    "@types/node": "^25.0.3",
    "eslint": "^9",
    "typescript": "^5"
  }
}
